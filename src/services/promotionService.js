const marketingStore=require('../data/marketingStore');
function matches(promo,p){const v=(promo.targetValue||'').toLowerCase();if(promo.targetType==='all')return true;if(promo.targetType==='product')return String(p.id).toLowerCase()===v||String(p.sku||'').toLowerCase()===v;if(promo.targetType==='category')return String(p.category||'').toLowerCase()===v;if(promo.targetType==='brand')return String(p.brand||'').toLowerCase()===v;return false;}
async function applyPromotion(product,isDistributor=false){const promos=await marketingStore.listPromotions({publicOnly:true});const applicable=promos.filter(x=>(!isDistributor||x.applyDistributor)&&matches(x,product));if(!applicable.length)return product;const best=applicable.sort((a,b)=>b.discountPercent-a.discountPercent)[0];const originalPrice=Number(product.price);const price=Math.round(originalPrice*(1-best.discountPercent/100)*100)/100;return {...product,originalPrice,price,promotion:{name:best.name,discountPercent:best.discountPercent}};}
module.exports={applyPromotion};
